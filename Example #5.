#include <stdio.h>
void swap1(int x, int y) {
int tmp = x;
x = y;
y = tmp;
}
void swap2(int *x, int *y) {
int tmp = *x;
*x = *y;
*y = tmp;
}
int main() {
int a = 1;
int b = 2;
printf("a: %d - b: %d\n", a, b);
swap1(a, b);
printf("a: %d - b: %d\n\n", a, b);
printf("a: %d - b: %d\n", a, b);
swap2(&a, &b);
printf("a: %d - b: %d\n", a, b);
return 0;
}
